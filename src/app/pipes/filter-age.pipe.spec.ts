import { FilterAgePipe } from './filter-age.pipe';

xdescribe('FilterAgePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAgePipe();
    expect(pipe).toBeTruthy();
  });
});
